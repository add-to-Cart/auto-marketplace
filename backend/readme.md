Responsibilities:

controllers → handle HTTP requests, validate input, call services

services → interact with Firebase/Cloudinary and implement business logic

routes → map API endpoints to controllers

config → secrets, API keys, Firebase and Cloudinary config
